<p style="color: green"><%= notice %></p>

<%= render @list %>

<div>
  <%= link_to "Edit", edit_list_path(@list) %> |
  <%= link_to "Back to todo lists", lists_path %>

  <%#<%= button_to "Destroy this list", @list, method: :delete %>
</div>
