<p style="color: green"><%= notice %></p>

<h1>My To Do List</h1>

<table>
  <thead>
    <th>Todo Item</th>
    <th>Completed</th>
    <th colspan="3"></th>
  </thead>
<%#<div id="lists">%>
  <% @lists.each do |list| %>
    <%#<%= render list %>
    <tr>
      <td><%= list.description %></td>
      <td><%= list.completed %></td>
      <td><%= link_to 'Show', list  %></td>
      <td><%= link_to 'Edit', edit_list_path(list)  %></td>
      <td><%= link_to "Destroy", @list, method: :delete %>
    </tr>
    <%#<p>
      <%= link_to "Show this to do", list %>
    <%#</p>%>
  <% end %>
<%#</div>%>
</table>

<%= link_to "New to do", new_list_path %>
